import React, { Component } from "react";

export default class MessagesList extends Component {
    render(){
        return(
            <React.Fragment>
                <div>Messages for this challenge.</div>
            </React.Fragment>
        )
    }
}