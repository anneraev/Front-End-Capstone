//handles the update loop for the app.

export default {
    startUpdate: function () {
        setInterval(() => {
            
        }, 60000)
    },

    stopUpdate: function() {
        clearInterval();
    }
}